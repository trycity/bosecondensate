// HandlerIndex.h

#ifndef _HANDLERINDEX_H_
#define _HANDLERINDEX_H_

namespace QMC {
    struct HandlerIndex {
        enum {HomogeneousPhi, SymHoPhi, EffectiveRangeF, HardSphereF, NonInteractingF};

        // taken from Modern C++ Design (Alexandrescu)
        template< int v >
        struct Int2Type {
            enum { value = v };
        };
    }; // end struct HandlerIndex
}; // end namespace QMC
#endif
